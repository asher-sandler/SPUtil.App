<Window x:Class="SPUtil.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copying Operation" 
        Height="230" Width="520" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F0F0F0">

    <Grid Margin="20,15,20,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <TextBlock x:Name="TxtStatus" 
                   Grid.Row="0"
                   Text="Preparing..." 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,10"
                   TextTrimming="CharacterEllipsis"/>

        <ProgressBar x:Name="PrgBar" 
                     Grid.Row="1"
                     Height="22" 
                     Minimum="0" Maximum="100" 
                     Foreground="#0078D7"/>

        <Grid Grid.Row="2" Margin="0,8,0,0">
            <TextBlock x:Name="TxtCount" Text="Processed: 0 / 0" HorizontalAlignment="Left" Foreground="#555"/>
            <TextBlock x:Name="TxtTime" Text="Remaining: calculating..." HorizontalAlignment="Right" Foreground="#555"/>
        </Grid>

        <Button x:Name="BtnCancel"
                Grid.Row="4"
                Content="Cancel" 
                Width="100" Height="30"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Click="BtnCancel_Click"
                Background="White"
                Margin="0,10,0,0"/>
    </Grid>
</Window>