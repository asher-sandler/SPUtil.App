<UserControl x:Class="SPUtil.App.Views.PagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <RowDefinition Height="Auto" /> <RowDefinition Height="*" />    <RowDefinition Height="5" />    <RowDefinition Height="*" />    <RowDefinition Height="Auto" /> </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="#F5F5F5" 
                BorderBrush="#CCCCCC" 
                BorderThickness="0,0,0,1" 
                Visibility="{Binding IsSourceMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                Padding="5"
                Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button  Command="{Binding GetAllPropertiesCommand}" 
                        Background="Transparent" 
                        BorderBrush="Transparent" 
                        Cursor="Hand">
                    <Button.ToolTip>
                        <ToolTip  Content="Вывести все Properties в лог (Output)" />
                    </Button.ToolTip>
 						
					<TextBlock Text="&#xE8FD;" Style="{StaticResource IconText}" Foreground="#107C10"/>
						
                </Button>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Foreground="DarkBlue" Margin="0,0,0,5" FontWeight="Bold"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Pages}" SelectedItem="{Binding SelectedPage}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="250" />
                <DataGridTextColumn Header="Путь" Binding="{Binding RelativeUrl}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Header="Веб-части выбранной страницы" Grid.Row="4">
            <DataGrid ItemsSource="{Binding WebParts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="200" />
                    <DataGridTemplateColumn Header="Свойства (Key: Value)" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding Properties}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="2">
                                                    <TextBlock Text="{Binding Key}" FontWeight="Bold" Foreground="#333" />
                                                    <TextBlock Text=": " />
                                                    <TextBlock Text="{Binding Value}" TextWrapping="Wrap" Foreground="#666" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>