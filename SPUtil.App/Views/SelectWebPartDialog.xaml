<Window x:Class="SPUtil.Views.SelectWebPartDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select WebPart"
        SizeToContent="Height" Width="560"
        MinHeight="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtHeader" Grid.Row="0"
                   TextWrapping="Wrap" Margin="0,0,0,10"
                   FontWeight="SemiBold"/>

        <DataGrid x:Name="GridCandidates" Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  MaxHeight="260"
                  MouseDoubleClick="GridCandidates_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Zone" Binding="{Binding ZoneId}" Width="100"/>
                <DataGridTextColumn Header="Position" Binding="{Binding VisualPosition}" Width="70"/>
                <DataGridTextColumn Header="Hidden" Binding="{Binding IsHidden}" Width="60"/>
                <DataGridTextColumn Header="Closed" Binding="{Binding IsClosed}" Width="60"/>
                <DataGridTextColumn Header="StorageKey" Binding="{Binding StorageKey}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnOk" Content="OK" Width="90" Margin="5,0"
                    Click="BtnOk_Click" IsDefault="True"
                    Background="#E1F5FE"/>
            <Button Content="Cancel" Width="90" Margin="5,0"
                    Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
